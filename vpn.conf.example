# openvpn-yubikey config
# copy to vpn.conf and fill in your values

# openvpn3 config name (from 'openvpn3 configs-list')
# import first with: openvpn3 config-import --config your.ovpn --name myconfig
CONFIG="myconfig"

# vpn credentials
USERNAME="your-username"
PASSWORD="your-password"

# yubikey oath account name
# run 'ykman oath accounts list' to find it
TOTP_ACCOUNT="OpenVPN:your-account@your-server.com"
